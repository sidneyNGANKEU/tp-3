apiVersion: v1
kind: Service
metadata:
  name: todo-api
  namespace: todo-api
  labels:
    app: todo-api
spec:
  type: ClusterIP
  selector:
    app: todo-api
  ports:
    - name: custom
      port: 8000
      targetPort: 8000
      protocol: TCP